# Root-level shim so a build that runs from the repository root still works.
#
# The real dependency list lives with the service that needs it. Keeping a
# second copy here guarantees the two drift apart, so this just points at it.
#
# Render uses `rootDir: apps/api`, which resolves `requirements.txt` to
# apps/api/requirements.txt directly and never reads this file.
-r apps/api/requirements.txt
