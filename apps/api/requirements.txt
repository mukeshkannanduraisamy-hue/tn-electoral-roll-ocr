# ---------------------------------------------------------------------------
# OCR backend dependencies, CPU build (Python 3.10 / 3.11). The default.
#
# This is `requirements-base.txt` plus the CPU framework. For the CUDA build
# see `requirements-gpu.txt`; the two are mutually exclusive, and why is
# explained in the base file.
#
# Install order matters on a cold environment: paddlepaddle (the framework)
# must land before paddleocr (the toolkit), or pip may resolve an
# incompatible pair. scripts/bootstrap.ps1 handles that.
# ---------------------------------------------------------------------------

paddlepaddle>=3.0.0b0,<3.2.0

-r requirements-base.txt
